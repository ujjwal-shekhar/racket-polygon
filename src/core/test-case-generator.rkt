;;; #lang racket

;;; (require (prefix-in tc: "core/test-case-generator.rkt"))

;;; ;; Example function to generate a random test case
;;; (define (generate-test-case)
;;;   (define a (random 100))
;;;   (define b (random 100))
;;;   (list (format "~a ~a" a b) (format "~a" (+ a b))))
